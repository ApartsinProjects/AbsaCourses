# API keys and credentials
.opeai.key
.gemini.key
.env
.env.*
*.key
*.token
labelstudio.json
argilla.json
.labelstudio.json
.argilla.json

# Agent state and tombstoned directories
.codex/
edu_DISABLED_DO_NOT_USE_20260403/

# Build artifacts and bytecode
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS and editor metadata
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/

# Derived figure rasters for the .docx build (regenerable from SVG)
paper/outputs/figures/_docx_png/

# Claude Code session state
.claude/

# Per-bucket per-seed sample-prediction dumps are large and regenerable from
# the audit CSV + bucket JSONLs + Modal harness. Keep the summary / per_aspect
# / metadata / thresholds / train.log artefacts which are load-bearing for the
# paper, ignore the prediction dumps.
paper/experiment_rounds/phase_d2_filtering_*/runs/*/run/artifacts/*sample_predictions*.jsonl

# Per-run Herath mapped JSONLs duplicate the canonical mapping; keep only the
# manifest.json + run-level summary/per_aspect/metadata.
paper/experiment_rounds/phase_d2_filtering_*/runs/*/herath_mapped_real_reviews.jsonl

# Bucket-corpus JSONLs are deterministic functions of the source corpus and
# the row_ids.txt list; keep the row_ids and manifest, ignore the materialized
# JSONLs (regenerable via paper/_d2_bucket.py).
paper/faithfulness_audit/buckets/*.jsonl

# Temporary calibration input (regenerable from existing GPT-5.2 audit details)
_d2_cal_input.jsonl

# EduRABSA raw downloads from HuggingFace (regenerable; the mapped JSONL
# in external_data/EduRABSA_mapped/ is what the experiment consumes).
external_data/EduRABSA/
external_data/EduRABSA_ASQE/

# html2tex bundle (regenerable from the source HTML via the html2tex skill).
_tmlr/


# Heavy pre-existing research data / artifacts (regenerable; never meant for git).
# Added 2026-07-02 during worktree consolidation.
external_data/Student_feedback_analysis_dataset/
paper/batch_requests/
paper/batch_results/
paper/generated_datasets/
paper/archive/
paper/benchmark_outputs/

# Cycle-2 intermediate generation/audit caches (regenerable; summaries + evidence tracked)
paper/outputs/n2_gen_*.jsonl
paper/outputs/n2_tuned_*.jsonl
paper/outputs/n2_judge.jsonl
paper/outputs/n3_gen_*.jsonl
paper/outputs/n3_audit_*.jsonl
paper/outputs/*_run.log

paper/outputs/n6b_pred_*.jsonl
paper/outputs/n6b_run.log
paper/_mathml.html
