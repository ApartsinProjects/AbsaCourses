# Live Todo

Date: 2026-04-03

## Unfinished Tasks

### High priority

- Complete the active multiseed stability round on the `10K / 20`-aspect corpus.
  - Active round status: `paper/experiment_rounds/phase_b1_multiseed_20260404/status.json`

- Refresh the remaining paper-facing benchmark outputs that still need rerun or regeneration under the corrected scripts.
  - real-transfer summaries
  - overlap-comparison summaries
  - manuscript tables/figures that depend on the refreshed evidence

- Run the remaining planned experiment phases from `integrated_paper_update_plan_20260403.md`.
  - baseline repair / tuning
  - full GPT-family completion where planned
  - faithfulness-aware filtering ablation
  - stronger external validation or stricter overlap checks
  - calibration / ranking diagnostics

### Medium priority

- Extend the GPT batch path or staged batch workflow so the full prompted family is executable.
  - remaining gap: `two-pass` and `aspect-by-aspect` in batch-evaluable form

- Regenerate manuscript tables/figures after multiseed and later experiment phases land.

- Integrate the new group-wise pedagogical analysis into the paper.
  - current artifacts:
    - `paper/outputs/tables/groupwise_summary.csv`
    - `paper/outputs/figures/groupwise_performance.svg`

### Low priority

- Clean or archive stale historical logs that still mention the disabled legacy `edu` path.
  - these are historical only, not live inputs

- Add one more consistency pass after the next evidence refresh.

## Already Completed

- Legacy `edu` dataset path disabled at the filesystem level.
- Experiment scripts aligned to the main `10K / 20`-aspect dataset.
- Batch-safe GPT evaluation path implemented and preserved.
- Faithfulness-audit batch path implemented and preserved.
- Low-level artifact preservation added for benchmark, transfer, overlap, and GPT batch evaluation runs.
- Phase A calibrated local benchmark rerun completed and aggregated into the paper-facing latest outputs.
- Group-wise pedagogical summary artifacts generated from the current benchmark and transfer outputs.
- Larger `250`-review faithfulness batch completed, consumed, and integrated into the manuscript-facing audit summary.

## Current Rule

- Do not use the disabled legacy dataset.
- Use Batch API for large GPT evaluation or faithfulness-audit jobs.
- Keep interactive OpenAI calls only for small smoke/debug workflows.
